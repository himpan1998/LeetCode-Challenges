const {
  c,
} = require("./leet-code-problems/30-days-of-javascript.js/2667. create-hello -world -function");
const { createCounter } = require("./leet-code-problems/2620.counter");

console.log(c());
console.log(createCounter(10));
